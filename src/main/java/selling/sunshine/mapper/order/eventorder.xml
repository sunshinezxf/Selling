<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="selling.event.order">
    <resultMap id="eventOrderVo" type="common.sunshine.model.selling.order.EventOrder">
        <result property="orderId" column="event_order_id"></result>
        <result property="donnePhone" column="donee_phone"></result>
        <result property="doneeAddress" column="donee_address"></result>
        <result property="orderStatus" column="order_status"
                typeHandler="selling.sunshine.handler.mybatis.order.OrderItemStatusHandler"></result>
        <result property="quantity" column="quantity"></result>
        <result property="blockFlag" column="block_flag"></result>
        <result property="createAt" column="create_time"></result>
        <association property="application" column="applicationId = application_id"></association>
        <association property="event" column="eventId = event_id"></association>
        <association property="goods" column="goodsId = goods_id"></association>
    </resultMap>

    <insert id="insert" parameterType="common.sunshine.model.selling.order.EventOrder">
        INSERT INTO event_order(event_order_id, donee_phone, donee_address, order_status, quantity, application_id, event_id, goods_id, block_flag, create_time)
        VALUES (#{orderId}, #{donnePhone}, #{doneeAddress}, #{orderStatus, typeHandler=org.apache.ibatis.type.EnumOrdinalTypeHandler}, #{quantity}, #{application.applicationId}, #{event.eventId}, #{goods.goodsId}, #{blockFlag}, #{createAt})
    </insert>

    <select id="query" parameterType="java.util.Map" resultMap="eventOrderVo">
        SELECT event_order_id, donee_phone, donee_address, order_status, quantity, block_flag, create_time,
        application_id, event_id, goods_id
        FROM event_order
        WHERE 1 = 1
        <if test="orderId != null">
            AND event_order_id = #{orderId}
        </if>
        <if test="doneePhone != null">
            AND donee_phone = #{doneePhone}
        </if>
        <if test="orderStatus != null">
            AND order_status = #{orderStatus}
        </if>
        <if test="blockFlag != null">
            AND block_flag = #{blockFlag}
        </if>
    </select>
</mapper>