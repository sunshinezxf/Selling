#parse("/common/util.vm")
<html lang="zh_CN">
<head>
    #parse("/customer/component/init.vm")
    <link rel="stylesheet" href="${path.concat('/material/css/customer/consult_order.css')}"/>
    <title>订单列表</title>
</head>
<body>
    <div class="hd">
        <h1 class="page_title">所有订单</h1>
    </div>
    <div class="bd">
        #if(${orderFromAgent}){
            #for(int i=0;i<${orderFromAgent.size()};i++){
            <div class="weui_panel weui_panel_access">
                <div class="weui_panel_hd">
                    <p>订单号:${orderFromAgent.get(i).orderItemId}</p>
                    <p>代理商:${orderFromAgent.get(i).agent.name}</p>
                </div>
                <div class="weui_panel_bd">
                    <div class="weui_media_box weui_media_text">
                        <h4 class="weui_media_title">商品名:${orderFromAgent.get(i).goods.name}</h4>
                        <p class="weui_media_desc">数量:${orderFromAgent.get(i).goodsQuantity}</p>
                    </div>
                    <div class="weui_media_box weui_media_text">
                        <h4 class="weui_media_title">收件人:${orderFromAgent.get(i).customer.name}</h4>
                        <p class="weui_media_desc">手机号:${orderFromAgent.get(i).customer.phone}</p>
                        <p class="weui_media_desc">地址:${orderFromAgent.get(i).receiveAddress}</p>
                    </div>
                    <div class="weui_media_box weui_media_text">
                        <h4 class="weui_media_title">共${orderFromAgent.get(i).goodsQuantity}件商品</h4>
                        <p class="weui_media_desc">合计：${orderFromAgent.get(i).orderItemPrice}元</p>
                    </div>
                </div>
                <!--<a href="javascript:void(0);" class="weui_panel_ft">查看更多</a>-->
            </div>
            #}
        #}
        
        #if(${orderFromCustomer}){
            #for(int j=0;j<${orderFromCustomer.size()};j++){
            <div class="weui_panel weui_panel_access">
                <div class="weui_panel_hd">
                    <p>订单号:${orderFromCustomer.get(j).orderId}</p>
                    <p>代理商:${orderFromCustomer.get(j).agent.name}</p>
                </div>
                <div class="weui_panel_bd">
                    <div class="weui_media_box weui_media_text">
                        <h4 class="weui_media_title">商品名:${orderFromCustomer.get(j).goods.name}</h4>
                        <p class="weui_media_desc">数量:${orderFromCustomer.get(j).quantity}</p>
                    </div>
                    <div class="weui_media_box weui_media_text">
                        <h4 class="weui_media_title">收件人:${orderFromCustomer.get(j).receiverName}</h4>
                        <p class="weui_media_desc">手机号:${orderFromCustomer.get(j).receiverPhone}</p>
                        <p class="weui_media_desc">地址:${orderFromCustomer.get(j).receiverAddress}</p>
                    </div>
                    <div class="weui_media_box weui_media_text">
                        <h4 class="weui_media_title">共${orderFromCustomer.get(j).quantity}件商品</h4>
                        <p class="weui_media_desc">合计：${orderFromCustomer.get(j).totalPrice}元</p>
                    </div>
                </div>
                <!--<a href="javascript:void(0);" class="weui_panel_ft">查看更多</a>-->
            </div>
            #}
        #}
        
        
        <div class="confirm_area">
			<button class="weui_btn weui_btn_primary" id="confirm">确定</button>
	    </div>
    </div>

    <!--
    <div class="weui_dialog_alert" id="address_dialog" style="display: none;">
        <div class="weui_mask"></div>
        <div class="weui_dialog">
            <div class="weui_dialog_hd"><strong class="weui_dialog_title">消息</strong></div>
            <div class="weui_dialog_bd"></div>
            <div class="weui_dialog_ft">
                <a href="javascript:;" class="weui_btn_dialog primary">确定</a>
            </div>
        </div>
    </div>
    -->
</body>
<script>
    $(document).ready(function(){
         //将地址数据存入dom节点中,并添加显示函数，必须放在地址字符串截取前
        //var address_cell = $("#order_item_list").eq(0).find(".address").parent(".weui_cell");
	    //address_cell.data("address", address_cell.find("#address").text());
		//address_cell.bind("click",$.showAddress);
		
        //地址字符串截取长度
        //$.addressBeautify();
    });
    
    //地址美化长度
    //$.addressBeautify = function(){
	//	var address = $("#address").html();
	//	$("#address").html($.utf8Substr(address,25));
	//}
	
	//$.showAddress = function(){
	//	$("#address_dialog").find(".weui_dialog_title").html($(this).data("address"));
	//	address_dom = this;
	//	$("#address_dialog").fadeIn();
	//}

	//$("#address_dialog").find(".weui_btn_dialog.primary").click(function(){
    //	$("#address_dialog").hide();
    //});
    
    $("#confirm").click(function(){
        WeixinJSBridge.call('closeWindow');
    });
    
</script>
</html>
